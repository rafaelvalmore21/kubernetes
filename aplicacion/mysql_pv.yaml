apiVersion: v1
kind: PersintenVolume
metadata: 
  name: mysql-pv
  namespace: logging
spec:
  capacity: 
    storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadwriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: standard
  hostPath:
    path: /path/on/host